name: testPGDB

on: [push]

jobs:
  testPGDB:
      runs-on: {ubuntu-latest,   r: 'release'}
      env:
        PG_DB: ${{ secrets.PG_DB }}
      steps:
        name: printPGDB
        run:
          echo "PG_DB=$PG_DB"
